name: Linter

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/base.yml
      - name: Run linter
        run: |
          flake8