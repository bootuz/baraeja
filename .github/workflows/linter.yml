name: Linter

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  linter:
    steps:
      - uses: ./.github/workflows/base.yml
      - name: Run linter
        run: |
          flake8